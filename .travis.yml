sudo: required
language: node_js
cache: yarn
node_js:
  - 8
services:
  - docker
env:
  - NODE_PORT=3000
before_install:
  - docker ps -a
install:
  - yarn
script:
  - yarn lint
  - yarn test
notifications:
  email:
    recipients:
      - yhl@1024hw.cn
    on_success: never # default: change
    on_failure: always # default: always
